const header=document.querySelector('[data-header]');const navToggle=document.querySelector('[data-nav-toggle]');const nav=document.querySelector('[data-nav]');const year=document.querySelector('[data-year]');if(year)year.textContent=new Date().getFullYear();const updateHeader=()=>{header?.classList.toggle('is-scrolled',window.scrollY>24)};updateHeader();window.addEventListener('scroll',updateHeader,{passive:true});if(navToggle&&nav){navToggle.addEventListener('click',()=>{const isOpen=navToggle.getAttribute('aria-expanded')==='true';navToggle.setAttribute('aria-expanded',String(!isOpen));nav.classList.toggle('is-open',!isOpen)});nav.addEventListener('click',event=>{if(event.target.closest('a')){navToggle.setAttribute('aria-expanded','false');nav.classList.remove('is-open')}});document.addEventListener('keydown',event=>{if(event.key==='Escape'&&nav.classList.contains('is-open')){navToggle.setAttribute('aria-expanded','false');nav.classList.remove('is-open');navToggle.focus()}})}const reducedMotion=window.matchMedia('(prefers-reduced-motion: reduce)').matches;const revealItems=[...document.querySelectorAll('.reveal')];if(!reducedMotion&&'IntersectionObserver'in window){const observer=new IntersectionObserver((entries,obs)=>{entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('is-visible');obs.unobserve(entry.target)}})},{threshold:.12,rootMargin:'0px 0px -40px'});revealItems.forEach(item=>observer.observe(item))}else{revealItems.forEach(item=>item.classList.add('is-visible'))}const tabs=[...document.querySelectorAll('[data-filter]')];const menuItems=[...document.querySelectorAll('[data-category]')];tabs.forEach(tab=>{tab.addEventListener('click',()=>{const filter=tab.dataset.filter;tabs.forEach(item=>{const selected=item===tab;item.classList.toggle('is-active',selected);item.setAttribute('aria-pressed',String(selected))});menuItems.forEach(item=>{item.hidden=filter!=='all'&&item.dataset.category!==filter})})});async function initAroma(){const canvas=document.querySelector('#aroma-canvas');const container=canvas?.parentElement;const saveData=navigator.connection?.saveData;if(!canvas||!container||reducedMotion||saveData||window.innerWidth<760)return;try{const THREE=await import('https://cdn.jsdelivr.net/npm/three@0.181.1/build/three.module.js');const renderer=new THREE.WebGLRenderer({canvas,alpha:true,antialias:false,powerPreference:'low-power'});renderer.setPixelRatio(Math.min(window.devicePixelRatio,1.5));const scene=new THREE.Scene();const camera=new THREE.PerspectiveCamera(45,1,.1,100);camera.position.z=8;const count=96;const positions=new Float32Array(count*3);const geometry=new THREE.BufferGeometry();for(let i=0;i<count;i+=1){const t=i/count;const plume=i%3;const xOffset=(plume-1)*1.1;positions[i*3]=xOffset+Math.sin(t*18+plume)*(.25+t*.45);positions[i*3+1]=-3.2+t*6.4+Math.sin(t*12+plume)*.16;positions[i*3+2]=Math.cos(t*10+plume)*.45}geometry.setAttribute('position',new THREE.BufferAttribute(positions,3));const material=new THREE.PointsMaterial({color:0xb79b67,size:.055,transparent:true,opacity:.62,sizeAttenuation:true});const points=new THREE.Points(geometry,material);points.rotation.z=-.18;scene.add(points);let raf;let visible=true;const sceneObserver=new IntersectionObserver(([entry])=>{visible=entry.isIntersecting},{threshold:0});sceneObserver.observe(container);const resize=()=>{const{clientWidth,clientHeight}=container;renderer.setSize(clientWidth,clientHeight,false);camera.aspect=clientWidth/Math.max(clientHeight,1);camera.updateProjectionMatrix()};resize();window.addEventListener('resize',resize,{passive:true});const start=performance.now();const tick=now=>{if(visible){const elapsed=(now-start)/1000;points.rotation.y=Math.sin(elapsed*.16)*.08;points.position.y=Math.sin(elapsed*.28)*.08;renderer.render(scene,camera)}raf=requestAnimationFrame(tick)};raf=requestAnimationFrame(tick);document.addEventListener('visibilitychange',()=>{if(document.hidden&&raf)cancelAnimationFrame(raf);if(!document.hidden)raf=requestAnimationFrame(tick)})}catch(error){console.info('Cafe Mera aroma enhancement unavailable.',error)}}window.addEventListener('load',()=>{window.setTimeout(initAroma,350)},{once:true});
